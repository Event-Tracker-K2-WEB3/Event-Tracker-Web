import SessionPageLoader from "../_components/SessionPageLoader";

export default function Loading() {
  return (
    <SessionPageLoader
      title="Loading session details"
      subtitle="Preparing the session information and live questions."
    />
  );
}